<!-- Blog Categories Well -->
<div class="well">
    <h4>分类目录</h4>
    <div class="row">
        <div class="col-lg-6">
            <ul class="list-unstyled">
              <% @category.each do |category| %>
              <li><%= link_to category.name, action: "index", filter_category: category.name %>
              </li>
              <% end %>
          </ul>
      </div>
    </div>
    <!-- /.row -->
  </div>
