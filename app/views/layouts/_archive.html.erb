<% if !Article.all.empty? %>
<form>
	<div class="list-group">
		<a class="list-group-item active">文章归档</a>  
		<% archive.each do |date| %>
		<%= link_to date, { action: "index", filter_archive: date }, class: "list-group-item" %>
		<% end %>		
	</div> 
</form>
<% end %>